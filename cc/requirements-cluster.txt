# Runtime deps for the cluster jobs: main.py, sweep.py, and the transfer_* producers.
#
# Deliberately NOT a lockfile mirror of ../requirements.txt:
#   * Version pins are floors, not `==`. The Alliance wheelhouse ships its own builds
#     (e.g. `jax==0.6.0+computecanada`), so exact upstream pins resolve to nothing under
#     `pip install --no-index`.
#   * pysr / matplotlib-only analysis scripts are excluded. PySR needs a Julia depot,
#     which is provisioned separately by cc/slurm/sr-run-starter.py's module loads.
#
# Install with cc/setup-venv.sh, which prefers cluster wheels and falls back to PyPI
# per package for anything the wheelhouse lacks.

# --- JAX + CUDA -------------------------------------------------------------
# jax_cuda12_* are the GPU plugin; on a CPU-only login node they install but stay unused.
jax>=0.6.0
jaxlib>=0.6.0
jax_cuda12_plugin>=0.6.0
jax_cuda12_pjrt>=0.6.0

# --- JAX ecosystem ----------------------------------------------------------
equinox>=0.13.0
optax>=0.2.5
optimistix>=0.1.0
jaxtyping>=0.3.2
chex>=0.1.90
orbax-checkpoint>=0.11.0
tensorstore>=0.1.70

# --- numerics / data --------------------------------------------------------
numpy>=2.2.0
scipy>=1.15.0
pandas>=2.2.0
pyarrow>=15.0.0
scikit-learn>=1.7.0
datasets>=4.0.0

# --- figures (util/logger.py and util/baselines.py import plotly at module scope) ---
plotly>=6.0.0
kaleido>=1.0.0
matplotlib>=3.10.0

# --- CLI / logging ----------------------------------------------------------
tyro>=0.9.17
wandb>=0.21.0
tqdm>=4.67.0

# --- dataset access (CheXpert / ImageNet-32 targets only) -------------------
kaggle>=2.0.0
