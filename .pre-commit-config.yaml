repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.5
    hooks:
      # Lint and auto-fix
      - id: ruff
        args: [--fix]
      # Format (replaces black)
      - id: ruff-format
